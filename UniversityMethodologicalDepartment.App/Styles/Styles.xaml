<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Compact inline hyperlink (closer to HTML &lt;a&gt; than default min-height padding). -->
    <Style x:Key="DetailHyperlinkButtonStyle"
           TargetType="HyperlinkButton"
           BasedOn="{StaticResource DefaultHyperlinkButtonStyle}">
        <Setter Property="MinHeight"
                Value="0" />
        <Setter Property="MinWidth"
                Value="0" />
        <Setter Property="Padding"
                Value="0" />
        <Setter Property="Margin"
                Value="0" />
        <Setter Property="HorizontalAlignment"
                Value="Left" />
        <Setter Property="HorizontalContentAlignment"
                Value="Left" />
        <Setter Property="VerticalAlignment"
                Value="Top" />
        <Setter Property="VerticalContentAlignment"
                Value="Top" />
    </Style>

    <!-- Used by FileContentBoxControl (copied from IPCM lab styles). -->
    <Style x:Key="StrongHeaderTextBoxStyle"
           TargetType="TextBox">
        <Setter Property="HeaderTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock Text="{Binding}"
                               FontWeight="Bold"
                               FontSize="14" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="SecondaryHintTextStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource BodyTextBlockStyle}">
        <Setter Property="Foreground"
                Value="{ThemeResource TextFillColorSecondaryBrush}" />
        <Setter Property="FontSize"
                Value="12" />
        <Setter Property="TextWrapping"
                Value="Wrap" />
    </Style>

    <x:Double x:Key="DetailPageDetailsColumnMinWidth">600</x:Double>
    <x:Double x:Key="DetailPageEditorColumnMinWidth">500</x:Double>

    <Style x:Key="DetailContentStackStyle"
           TargetType="StackPanel">
        <Setter Property="Spacing"
                Value="12" />
    </Style>

    <Style x:Key="DetailSectionPanelStyle"
           TargetType="StackPanel">
        <Setter Property="Spacing"
                Value="8" />
    </Style>

    <Style x:Key="DetailFieldPanelStyle"
           TargetType="StackPanel">
        <Setter Property="Spacing"
                Value="4" />
    </Style>

    <Style x:Key="DetailFieldLabelStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource CaptionTextBlockStyle}">
        <Setter Property="Foreground"
                Value="{ThemeResource TextFillColorSecondaryBrush}" />
    </Style>

    <Style x:Key="DetailStatusMessageTextStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource BodyTextBlockStyle}">
        <Setter Property="TextWrapping"
                Value="WrapWholeWords" />
    </Style>

    <Style x:Key="DetailFieldValueStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource BodyTextBlockStyle}">
        <Setter Property="TextWrapping"
                Value="Wrap" />
        <Setter Property="LineHeight"
                Value="22" />
    </Style>

    <Style x:Key="DetailSectionTitleStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource BodyStrongTextBlockStyle}">
        <Setter Property="Margin"
                Value="0,4,0,0" />
    </Style>

    <Style x:Key="DetailPageCardBorderStyle"
           TargetType="Border">
        <Setter Property="Padding"
                Value="20" />
        <Setter Property="HorizontalAlignment"
                Value="Center" />
        <Setter Property="CornerRadius"
                Value="10" />
        <Setter Property="BorderThickness"
                Value="1" />
        <Setter Property="BorderBrush"
                Value="{ThemeResource CardStrokeColorDefaultBrush}" />
        <Setter Property="Background"
                Value="{ThemeResource CardBackgroundFillColorDefaultBrush}" />
        <Setter Property="MaxWidth"
                Value="800" />
        <Setter Property="MinWidth"
                Value="500" />
    </Style>

    <Style x:Key="DetailPageScrollViewerStyle"
           TargetType="ScrollViewer">
        <Setter Property="Margin"
                Value="36,24,36,16" />
        <Setter Property="HorizontalContentAlignment"
                Value="Center" />
        <Setter Property="HorizontalScrollMode"
                Value="Disabled" />
        <Setter Property="VerticalScrollMode"
                Value="Auto" />
    </Style>

    <Style x:Key="DetailPageGridSplitterStyle"
           TargetType="Control">
        <Setter Property="HorizontalAlignment"
                Value="Stretch" />
        <Setter Property="VerticalAlignment"
                Value="Stretch" />
        <Setter Property="Background"
                Value="{ThemeResource DividerStrokeColorDefaultBrush}" />
    </Style>

    <Style x:Key="DetailPageRootGridStyle"
           TargetType="Grid">
        <Setter Property="MinWidth"
                Value="600" />
    </Style>

    <Style x:Key="HomeQuickAccessButtonStyle"
           TargetType="Button"
           BasedOn="{StaticResource DefaultButtonStyle}">
        <Setter Property="HorizontalAlignment"
                Value="Left" />
        <Setter Property="HorizontalContentAlignment"
                Value="Left" />
        <Setter Property="VerticalContentAlignment"
                Value="Top" />
        <Setter Property="MinHeight"
                Value="84" />
        <Setter Property="Padding"
                Value="12,10" />
        <Setter Property="CornerRadius"
                Value="10" />
        <Setter Property="BorderThickness"
                Value="1" />
        <Setter Property="Background"
                Value="{ThemeResource CardBackgroundFillColorDefaultBrush}" />
        <Setter Property="BorderBrush"
                Value="{ThemeResource CardStrokeColorDefaultBrush}" />
        <Setter Property="Foreground"
                Value="{ThemeResource TextFillColorPrimaryBrush}" />
        <Setter Property="UseSystemFocusVisuals"
                Value="True" />
    </Style>

    <Style x:Key="HomeQuickAccessTitleStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource BodyStrongTextBlockStyle}">
        <Setter Property="TextWrapping"
                Value="WrapWholeWords" />
    </Style>

    <Style x:Key="HomeQuickAccessDescriptionStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource BodyTextBlockStyle}">
        <Setter Property="Foreground"
                Value="{ThemeResource TextFillColorSecondaryBrush}" />
        <Setter Property="TextWrapping"
                Value="WrapWholeWords" />
    </Style>

    <Style x:Key="AdminPageRootGridStyle"
           TargetType="Grid">
        <Setter Property="Padding"
                Value="24" />
        <Setter Property="RowSpacing"
                Value="12" />
    </Style>

    <Style x:Key="AdminToolbarStatusTextStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource BodyTextBlockStyle}">
        <Setter Property="VerticalAlignment"
                Value="Center" />
        <Setter Property="Foreground"
                Value="{ThemeResource TextFillColorSecondaryBrush}" />
    </Style>

    <Style x:Key="AdminMasterDetailGridStyle"
           TargetType="Grid">
        <Setter Property="Margin"
                Value="0,8,0,0" />
        <Setter Property="ColumnSpacing"
                Value="16" />
    </Style>

    <Style x:Key="AdminDetailsCardBorderStyle"
           TargetType="Border">
        <Setter Property="Padding"
                Value="12" />
        <Setter Property="CornerRadius"
                Value="8" />
        <Setter Property="BorderBrush"
                Value="{ThemeResource CardStrokeColorDefaultBrush}" />
        <Setter Property="BorderThickness"
                Value="1" />
        <Setter Property="Background"
                Value="{ThemeResource CardBackgroundFillColorDefaultBrush}" />
    </Style>

    <Style x:Key="AdminSectionTitleTextStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource SubtitleTextBlockStyle}">
        <Setter Property="FontWeight"
                Value="SemiBold" />
    </Style>

    <Style x:Key="AdminModeHintTextStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource CaptionTextBlockStyle}">
        <Setter Property="Foreground"
                Value="{ThemeResource TextFillColorSecondaryBrush}" />
    </Style>

</ResourceDictionary>
