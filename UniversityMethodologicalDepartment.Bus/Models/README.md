# Models

Папка зарезервирована для DTO и других транзитивных моделей (не сущностей БД). Сущности хранятся в `Entities` и могут перегенерироваться scaffold-ом; репозитории и фасад возвращают сущности, DTO можно ввести позже.
